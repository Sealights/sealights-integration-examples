var sealights = require('../test-listener/api');
var ConfigLoader = require('../common/config-process/config-loader').ConfigLoader;
var agentConfig = new ConfigLoader().loadAgentConfiguration();
var requestData = {
    customerId: agentConfig.customerId.value,
    appName: agentConfig.appName.value,
    environment: agentConfig.labId.value,
    buildName: agentConfig.build.value,
    branchName: agentConfig.branch.value,
    executionIds: [process.argv[2]]
};
var httpConfigData = {
    server: agentConfig.server.value,
    proxy: agentConfig.proxy.value,
    token: agentConfig.token.value
};
console.log("[Sealights] Mocha did not exit gracefully, forcing execution to end");
sealights.endExecution(requestData, httpConfigData, function (err) {
    if (err) {
        console.error(err);
    }
});
//# sourceMappingURL=end-execution.js.map