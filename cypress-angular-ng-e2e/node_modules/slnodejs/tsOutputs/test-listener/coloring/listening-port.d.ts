import { IColoringProvider } from './icoloring-provider';
import { AgentConfig } from '../../common/config-process/config';
import { StateTracker } from '../../common/state-tracker';
import { Logger } from '../../common/contracts';
/**
 * Created by nadav on 19/03/2017.
 */
/**
 * This class creates an HTTP server that can be used in order to color the agent.
 * We'll probably need that when the client's uses something that is unsupported (ie, not HTTP).
 * The actual color will be set by the HttpColoring class.
 * */
export declare class ListeningPort implements IColoringProvider {
    private server;
    setup(config: AgentConfig, stateTracker: StateTracker, log: Logger): void;
    teardown(): void;
    private setupListeningPort;
    private handleRequest;
    private stopListening;
}
