import { AgentConfig } from './config';
export declare class ConfigLoader {
    private logger?;
    constructor(logger?: any);
    loadAgentConfiguration(initialJsonConfig: Record<string, any>): AgentConfig;
    private printConfiguration;
    private resolveUsingOtel;
    private loadConfigFromToken;
}
