import { ICollectionInterval } from './istanbul-to-footprints-contract';
export declare class CollectionInterval {
    private start;
    private end;
    private intervalInSeconds;
    constructor(intervalInMS: number);
    next(): void;
    toJson(): ICollectionInterval;
    private toSeconds;
}
