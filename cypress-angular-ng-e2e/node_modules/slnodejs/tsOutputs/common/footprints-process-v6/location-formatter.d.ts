import { IPosition } from './contracts';
import { Logger } from '../contracts';
/**
 * Format location of coverage element in instrumented file (reduced and non-reduced)
 */
export declare function formatLocation(position: IPosition, logger: Logger): string;
