import { HitsCollector } from './hits-collector';
import { IIstanbulProcessMetrics } from './contracts';
/**
 * Used in case of coverage data collected remotely and set from outside (e.g instrumented browser code)
 */
export declare class RemoteHitsCollector extends HitsCollector {
    getGlobalCoverageObject(): IIstanbulProcessMetrics;
    /**
     * Each time coverage object set it contains the complete coverage data for the test, no need to calculate the diff
     * @param currentCounters
     * @protected
     */
    protected updateCoverageSnapshot(currentCounters: IIstanbulProcessMetrics): void;
}
