import { IPositionFull, IUniqueIdParts } from './contracts';
import { Logger as logger } from '../contracts';
export declare class SourceMapData {
    private pathToConsumer;
    private logger;
    constructor(logger: logger);
    getSourcePosition(relativePath: string, absolutePath: string, start: IPositionFull, projectRoot: string, slMapping: {
        [key: string]: string;
    }): IUniqueIdParts;
    private getSourceMapForModule;
}
