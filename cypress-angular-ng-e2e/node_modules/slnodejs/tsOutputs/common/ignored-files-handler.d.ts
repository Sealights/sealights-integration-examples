import { Logger } from './contracts';
export declare class IgnoredFilesHandler {
    static readonly SCAN_IGNORE_FILE_NAME = ".slignore";
    static readonly GENERATED_IGNORE_FILE_NAME = ".slignore.generated";
    private static readonly MAX_ENTRIES_TO_LOG;
    private slIgnoreFilePath;
    private outputPath;
    private excludedPaths;
    private filename;
    private rawPattern;
    isUsingNycConfig: boolean;
    private logger;
    private ignoredPatterns;
    private loggedEntries;
    constructor(slIgnoreFilePath: string, outputPath: string, excludedPaths: string, filename: string, logger: Logger);
    private initIgnorePatterns;
    private getRawPatterns;
    private addCliOptionExcludedPaths;
    private readFromSlIgnoreFile;
    private getFormattedPattern;
    private addExcludedPath;
    private getDefaultPattern;
    private readFromNycConfig;
    private readNycConfig;
    private createExcludePatternFromNyc;
    denies(input: any): any;
    accepts(input: any): boolean;
}
