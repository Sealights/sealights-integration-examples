/**
 * Enable getting build session data from backend by lab id.
 * If build-session-id or build-session-id file provided, the provided one will be used.
 */
import { BuildSessionData } from 'common/http/contracts';
import { BuildSessionDataBuilder } from './build-session-data-builder';
import { IMochaArgs, IStartExecutionArgs, ITestExecutionArgs } from '../../cli-parse/contracts';
export declare class TestRunnerBuildSessionDataBuilder extends BuildSessionDataBuilder {
    build(cmd: IMochaArgs | IStartExecutionArgs | ITestExecutionArgs): Promise<BuildSessionData>;
    private shouldParseFromLabId;
}
