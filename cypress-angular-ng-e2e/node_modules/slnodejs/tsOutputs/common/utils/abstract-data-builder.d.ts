import { BuildSessionData } from '../http/contracts';
import { IArgs, ITokenData, Logger } from '../contracts';
/**
 * Base class for reading data (token or buildSessionId) from file.
 */
export declare abstract class AbstractDataBuilder {
    protected logger: Logger;
    abstract build(cmd: IArgs): ITokenData | Promise<BuildSessionData>;
    protected getFromFile(cmd: any, key: string): string;
}
