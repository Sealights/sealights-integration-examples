import { BackendProxy } from '../http/backend-proxy';
import { AbstractDataBuilder } from './abstract-data-builder';
import { BuildSessionData } from '../http/contracts';
import { IConfigArgs, IGeneralArgs } from '../contracts';
/**
 * Build the build session data (app, branch and build).
 * For config commands, returns the values from CLI.
 * For other commands, resolves using backend.
 */
export declare class BuildSessionDataBuilder extends AbstractDataBuilder {
    protected backendProxy: BackendProxy;
    constructor(backendProxy: BackendProxy);
    build(cmd: IGeneralArgs): Promise<BuildSessionData>;
    parseFromCli(cmd: IConfigArgs): BuildSessionData;
    private parseFromBuildSessionId;
    private mapBuildSessionResponse;
    private getRawBuildSessionId;
}
