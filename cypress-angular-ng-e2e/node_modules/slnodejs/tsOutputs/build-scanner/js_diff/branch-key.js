module.exports = function (filename, startLineAndColumn, branchIndex) {
    var keyParts = [];
    keyParts.push(filename);
    if (startLineAndColumn) {
        keyParts.push(startLineAndColumn.join(','));
    }
    keyParts.push(branchIndex);
    var wholeKey = keyParts.join('|');
    return wholeKey;
};
//# sourceMappingURL=branch-key.js.map