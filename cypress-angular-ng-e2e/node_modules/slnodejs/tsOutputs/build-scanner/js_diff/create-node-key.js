function createNodeKey(node) {
    var start = node.loc.start;
    var end = node.loc.end;
    var key = [start.line, start.column, end.line, end.column].join("|");
    return key;
}
module.exports.createNodeKey = createNodeKey;
//# sourceMappingURL=create-node-key.js.map