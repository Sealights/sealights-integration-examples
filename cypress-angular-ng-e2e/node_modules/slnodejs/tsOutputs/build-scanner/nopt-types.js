module.exports = {
    git: {
        type: "scm",
        validate: function (data, key, val) {
            val = val || "";
            var v = val.toString().toLowerCase();
            switch (v) {
                case "git":
                    data[key] = v;
                    return true;
                case "none":
                    data[key] = v;
                    return true;
                default:
                    return false;
            }
        }
    }
};
//# sourceMappingURL=nopt-types.js.map