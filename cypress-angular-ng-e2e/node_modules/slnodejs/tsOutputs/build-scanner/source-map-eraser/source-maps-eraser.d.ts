import { Logger } from '../../common/contracts';
import { SkipCopySourceMaps } from './skip-copy-source-maps';
export declare class SourceMapsEraser extends SkipCopySourceMaps {
    protected logger: Logger;
    protected workspace: string;
    protected pattern: string;
    constructor(logger: Logger, workspace: string, pattern?: string);
    isShouldSkipCopy(path: string): boolean;
    remove(): Promise<void>;
}
