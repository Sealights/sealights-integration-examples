import { Logger } from '../../common/contracts';
import { ISourceMapsEraser } from './interface';
export declare class SourceMapEraserFactory {
    static createSourceMapEraserByMode(mode: string, logger: Logger, workspace: string, pattern?: string): ISourceMapsEraser;
}
